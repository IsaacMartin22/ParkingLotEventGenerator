# EventSpammer

EventSpammer is a configurable Java tool for continuously sending API requests to a server.

It can be used to repeatedly create, update, delete, or fetch data through an HTTP API.

## Requirements

- Java 21+
- Maven 3.9+

## Build


Build: ``mvn clean package``

Run: ``java -jar target/event-spammer-1.0.0.jar``

